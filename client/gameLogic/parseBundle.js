'use strict';

export default function parseBundle(obj) {
  const bundle = obj.bundle;
  const deckData = {};
  for (let i = 0; i < 3; i++) {
    const questionData = {};
    for (let j = 0; j < 5; j++) {
      questionData;
    }
  }
  for (const deck of bundle) {
    console.log(deck);

  }
}
